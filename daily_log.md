# Daily Log

## 2026-04-14
- **研究結果參數擴充**：為研究提案新增了三個核心評估參數，提升提案的專業度與可參考性。
  - **新增參數欄位**：在 `proposals` 資料結構中為每個提案新增了 `wins`（勝場數）、`novelty`（新穎性）及 `methodology`（使用的方法論）欄位。
  - **提案卡片 UI 更新**：在 `ui.js` 的 `showResults` 邏輯中新增了 `proposal-stats` 區塊，以橫向排版展示上述三個參數。
  - **視覺樣式優化**：在 `main.css` 中為統計數據設計了專屬的 `stat-item` 樣式，採用輕量化的背景色與精緻的標籤設計，確保資訊清晰易讀。
  - **資料豐富化**：為現有的 10 個模擬提案補充了具體的參數數值，例如「勝場數：12」、「新穎性：98%」（改用百分比顯示）、「方法論：多模態融合深度學習」等，增強展示效果。

## 2026-04-13
- **新增會員資訊區塊與儲值模擬功能**：在主介面左上角（側邊欄頂端）實作了會員系統，提供個人化資訊展示。
  - **側邊欄會員標記**：新增 `member-badge` 區塊，即時顯示會員頭像、姓名、身分標籤（專業版）及目前點數。
  - **會員帳戶中心彈窗**：設計了專屬彈窗（Modal），展示詳細會員資料、ID 及大型餘額卡片。
  - **模擬儲值流程**：提供三種點數套餐，並實作模擬儲值邏輯（包含確認視窗與點數即時更新 UI）。
  - **狀態與 UI 整合**：在 `state.js` 中新增會員資料物件，並在 `ui.js` 實作資料同步與彈窗切換邏輯。
  - **樣式細節優化**：為會員相關區塊添加了懸停效果、分隔線、毛玻璃背景彈窗及圓角陰影設計。
- **新增參考文獻展開功能**：
  - **提案卡片擴充**：在 Step 4 的每個研究提案卡片內新增了「參考文獻」區塊。
  - **互動式展開按鈕**：實作了一個切換按鈕，預設顯示「顯示參考文獻 (2)」，點擊後可展開查看詳細的模擬文獻列表，並動態更新按鈕文字與箭頭方向。
  - **視覺與動畫優化**：為參考文獻列表添加了滑入（Slide Down）動畫效果與專屬的條列式樣式，確保介面整潔且具備層次感。
  - **動態事件綁定**：在 `ui.js` 中新增了 `toggleReferences` 邏輯，確保每個卡片的展開狀態相互獨立。
- **新增自訂研究主題功能**：
  - **上傳階段自訂**：在 Step 1 上傳論文後，新增一個「研究主題」輸入欄位，系統會自動根據檔名預填，並允許使用者自由命名。
  - **結果階段重命名**：在 Step 4 研究提案頁面，將原有的靜態標題改為可編輯的輸入框，使用者在查看結果時仍能隨時調整研究主題。
  - **雙向同步與狀態管理**：在 `state.js` 中新增 `researchSubject` 欄位，並實作跨頁面、跨輸入框的即時同步邏輯。
  - **UI 互動優化**：為 Step 4 的編輯框添加了懸停顯示的編輯圖示及焦點底線動畫，提升操作引導性。
- **提案結果顯示原論文標題**：
  - **介面更新**：在 Step 4 的結果頁面頂部新增了 `source-paper-tag` 標籤，用以顯示使用者上傳的原始論文檔名。
  - **動態資料綁定**：在 `ui.js` 的 `showResults` 邏輯中，自動從全域狀態提取 `fileName` 並渲染至介面，增強研究脈絡的完整性。
  - **專屬視覺樣式**：為原論文標籤設計了輕量化的藍色調風格，與「研究提案」標題形成清晰的層次感。

- **新增 Landing Page 入口頁面**：在應用程式主畫面之前建立了一個引人入勝的登陸頁面，提升產品的第一印象。
  - **首頁視覺設計**：在 `src/index.html` 中加入了包含標誌（SVG 繪製）、標題及副標題的 `landingPage` 容器。
  - **響應式樣式優化**：在 `src/css/main.css` 中實作了全螢幕背景、漸入動畫（Fade-In）以及具有陰影效果的「開始使用」按鈕。
  - **頁面切換邏輯**：在 `src/js/app.js` 中新增了初始化邏輯，點擊按鈕後隱藏 Landing Page 並顯示主應用程式區域（`mainApp`）。
  - **結構重整**：將原有的主介面內容包裝在 `mainApp` 容器中，並預設為隱藏，確保順暢的導覽流程。

## 2026-04-13 (Earlier)
- **模組化現代網頁架構重構**：將原有的單一 HTML 檔案拆解為具備「關注點分離」特性的現代架構，提升可維護性。
  - **建立 src 目錄結構**：區分 `css` 與 `js/modules`，使資源分類清晰。
  - **樣式抽取與優化**：將所有 CSS 移至 `src/css/main.css`，並引入 **CSS 變數**（如 `--primary-color`, `--bg-card` 等），方便統一調整視覺風格。
  - **資料與邏輯分離**：建立 `src/js/modules/data.js` 存放理論清單、樣式定義及模擬提案內容，未來可輕鬆對接後端 API。
  - **狀態管理實作**：建立 `src/js/modules/state.js` 集中管理應用程式狀態（如當前步驟、選擇的理論等），避免全域變數污染。
  - **UI 與互動邏輯解耦**：將 DOM 渲染與事件處理封裝至 `src/js/modules/ui.js`，並透過 `src/js/app.js` 進行初始化與入口管理。
  - **採用 ES Modules**：使用現代瀏覽器支援的 `import/export` 語法，實現腳本的模組化載入。
- **修復與優化**：
  - **重置 Spinner 狀態**：修正「重新生成」後載入動畫消失的問題。
  - **實作敗部復活切換**：將原有的 `alert` 改為實際切換面板顯示，提升使用者體驗。
  - **強化重複渲染穩定性**：優化 `showResults` 邏輯，確保每次生成結果都能正確清空舊內容。
- **檔案維護與修補**：
  - **重命名舊版檔案**：將根目錄下無副檔名的 `html` 檔案更名為 `backup_old_version.html`，以修復 IDE 將其誤判為 CSS 檔案而產生的語法錯誤報錯。

## 2026-04-10
- **新增左側歷史紀錄側邊欄**：將整體介面調整為左右排版（`.layout-container`），並新增最近生成的計畫清單，以展示過往紀錄。
- **新增快捷套餐方案**：在設定面板（Step 2）頂端加入「簡易」、「一般」、「進階」等按鈕，包含點擊連動邏輯（自動選取對應的創新理論、種子數與迭代次數）。
- **實作點數計費顯示**：在預估時間區域旁加入點數顯示（`.pts-display`），點數由所選理論數量、種子數、迭代數綜合計算。
- **修改敗部復活功能**：點擊敗部復活按鈕後不再出現展開選單，改為彈窗顯示「此功能開發中」。
